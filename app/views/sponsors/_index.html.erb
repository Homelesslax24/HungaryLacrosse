<div class="jumbotron center no-margin-top", id="sponsors"><!-- Sponsors -->
	<h2 class="section-title" style="color: white;">Support our Sponsors</h2>
	<div class="container-fluid">
		<div class="jumbotron col-xs-12">
			<% @sponsors.each do |sponsor| %>
				<div class="col-xs-12 col-md-6 col-lg-4 sponsor-item">
					<%= link_to image_tag(sponsor.logo, :alt => sponsor.name + " Logo"), sponsor.website, {class: "hvr-float-shadow sponsor-logo", target: "_blank"} %>
				</div>
			<% end %>
		</div>
	</div>
</div>