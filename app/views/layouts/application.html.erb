<!DOCTYPE html>
<html>
  <head>
    <title>Hungarian Lacrosse Association | The Official Site of Lacrosse in Hungary</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>
  <body>
    <div id="fb-root"></div> <!-- XXXX Need to move this to only call it on the home page -->
      <script>(function(d, s, id) {
        var js, fjs = d.getElementsByTagName(s)[0];
        if (d.getElementById(id)) return;
        js = d.createElement(s); js.id = id;
        js.src = "//connect.facebook.net/en_US/sdk.js#xfbml=1&version=v2.5";
        fjs.parentNode.insertBefore(js, fjs);
      }(document, 'script', 'facebook-jssdk'));</script> <!-- end move section -->

  	<div>
  		<%= render 'layouts/header' %>
  	</div>
  	
  	<div class="container">
      <% flash.each do |name, msg| %>
        <%= content_tag(:div, msg, class: "row col-lg-8 col-lg-offset-1 alert alert-info first-item", style: "position:fixed;") do %>
          <%= msg %>
          <button type="button" class="close" data-dismiss="alert">&times;</button>
        <% end %>
      <% end %>
    </div>

    <div>
  	  <%= yield %>
    </div>
  		
  	<%= render 'layouts/footer' %>
  		
  </body>
</html>
